.program od_pwm

; NPN open-collector PWM bank. GPIO -- 1k -- NPN base; collector is the
; fan PWM pin (5V pull-up). GPIO 1 = NPN on = fan PWM low.
; Each FIFO word is pin values for GPIO 0-15. DMA feeds 256 steps at 25 kHz.

.wrap_target
    out pins, 32
.wrap
